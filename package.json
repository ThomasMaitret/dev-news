{
  "name": "dev-news",
  "version": "1.0.0",
  "description": "Developer News",
  "main": "main.js",
  "browserslist": [
    "maintained node versions"
  ],
  "scripts": {
    "start": "rimraf dist && parcel index.html",
    "build": "rimraf dist && parcel build index.html --public-url . --no-source-maps --experimental-scope-hoisting"
  },
  "keywords": [],
  "author": "Thomas Maitret-Richard",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-inliner": "^1.0.10",
    "rimraf": "^3.0.0"
  },
  "dependencies": {
    "darkmode-js": "^1.5.3"
  }
}
